apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pv-db-claim
  labels:
    app: vprodb
spec:
  storageClassName: default
  accessModes:
    - ReadWriteOnce
  resource:
    requests:
      storage: 3Gi
